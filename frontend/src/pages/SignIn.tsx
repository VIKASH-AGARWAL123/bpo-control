import {FormEvent,useState} from 'react'
import {Link,useNavigate} from 'react-router-dom'
import {useAuth} from '../context/AuthContext'
export default function SignIn(){const {signin}=useAuth();const nav=useNavigate();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setError('');setBusy(true);try{await signin(email,password);nav('/dashboard')}catch(err:any){setError(err.response?.data?.message||'Sign in failed.')}finally{setBusy(false)}};return <div className="min-h-screen bg-slate-50"><div className="mx-auto grid min-h-screen max-w-6xl grid-cols-1 lg:grid-cols-2"><div className="hidden flex-col justify-center p-12 lg:flex"><div className="max-w-lg"><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-slate-900 font-bold text-white">B</div><span className="font-bold">BPO Control</span></div><h1 className="mt-10 text-5xl font-black tracking-tight">Your operations control room.</h1><p className="mt-5 text-lg leading-8 text-slate-600">Manage task execution, SLAs, queues, teams and client operations without the overhead of a generic project-management suite.</p></div></div><div className="flex items-center justify-center p-6"><form onSubmit={submit} className="card w-full max-w-md p-8"><h2 className="text-2xl font-bold">Welcome back</h2><p className="mt-2 text-sm text-slate-500">Sign in to your organization.</p>{error&&<div className="mt-5 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</div>}<div className="mt-7 space-y-4"><div><label className="mb-1 block text-sm font-medium">Email</label><input className="input" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><div><label className="mb-1 block text-sm font-medium">Password</label><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></div><button disabled={busy} className="btn btn-primary w-full py-3">{busy?'Signing in...':'Sign in'}</button></div><p className="mt-6 text-center text-sm text-slate-500">No account? <Link className="font-semibold text-slate-900" to="/signup">Create an organization</Link></p></form></div></div></div>}
