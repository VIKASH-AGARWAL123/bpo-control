import {useEffect,useState} from 'react'
import {CheckCircle2,Clock3,Users,AlertTriangle,RefreshCw} from 'lucide-react'
import api from '../services/api'
import type {Dashboard as DashboardData} from '../types'
export default function Dashboard(){const [data,setData]=useState<DashboardData|null>(null);const [error,setError]=useState('');const load=async()=>{try{const r=await api.get('/dashboard');setData(r.data)}catch(e:any){setError(e.response?.data?.message||'Unable to load dashboard')}};useEffect(()=>{load()},[]);const stats=data?.stats;return <div className="space-y-6"><div className="flex items-center justify-between"><div><h1 className="text-2xl font-bold">Operations Dashboard</h1><p className="mt-1 text-sm text-slate-500">Live view of your organization's operational workload.</p></div><button className="btn btn-secondary" onClick={load}><RefreshCw size={16} className="mr-2"/>Refresh</button></div>{error&&<div className="rounded-lg bg-red-50 p-4 text-sm text-red-700">{error}</div>}<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{[['Total Tasks',stats?.total_tasks??0,'Across all work',null],['Completed',stats?.completed??0,'Completed tasks',null],['SLA At Risk',stats?.sla_at_risk??0,'Needs attention','danger'],['SLA Breached',stats?.sla_breached??0,'Requires escalation','danger']].map(([t,v,d,s])=><div className="card p-5" key={String(t)}><div className="text-sm text-slate-500">{t}</div><div className={`mt-2 text-3xl font-black ${s?'text-red-600':'text-slate-900'}`}>{v}</div><div className="mt-1 text-xs text-slate-400">{d}</div></div>)}</div><div className="grid gap-6 lg:grid-cols-4"><div className="card p-5"><div className="flex items-center gap-3"><CheckCircle2 size={20}/><div><div className="text-xs text-slate-500">SLA status</div><div className="text-xl font-bold">{data?.sla_breakdown?.met??0} met</div></div></div></div><div className="card p-5"><div className="flex items-center gap-3"><Clock3 size={20}/><div><div className="text-xs text-slate-500">Pending</div><div className="text-xl font-bold">{stats?.pending??0}</div></div></div></div><div className="card p-5"><div className="flex items-center gap-3"><Users size={20}/><div><div className="text-xs text-slate-500">Unassigned</div><div className="text-xl font-bold">{stats?.unassigned??0}</div></div></div></div><div className="card p-5"><div className="flex items-center gap-3"><AlertTriangle size={20}/><div><div className="text-xs text-slate-500">Overdue</div><div className="text-xl font-bold">{stats?.overdue??0}</div></div></div></div></div><div className="card overflow-hidden"><div className="border-b border-slate-200 p-5"><h2 className="font-bold">Recent tasks</h2><p className="text-xs text-slate-500">Latest operational work in this tenant.</p></div><div className="overflow-x-auto"><table className="w-full text-sm"><thead className="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th className="px-5 py-3">Task</th><th className="px-5 py-3">Status</th><th className="px-5 py-3">Priority</th><th className="px-5 py-3">SLA</th></tr></thead><tbody className="divide-y divide-slate-100">{data?.recent_tasks?.map(t=><tr key={t.id}><td className="px-5 py-4"><div className="font-medium">{t.title}</div><div className="text-xs text-slate-400">{t.task_number}</div></td><td className="px-5 py-4">{t.status}</td><td className="px-5 py-4">{t.priority}</td><td className="px-5 py-4">{t.sla_status}</td></tr>)}</tbody></table></div></div></div>}
