import {useEffect,useState} from 'react';import api from '../services/api';export default function Reports(){const [d,setD]=useState<any>();useEffect(()=>{api.get('/reports/summary').then(r=>setD(r.data))},[]);return <div className="space-y-5"><div><h1 className="text-2xl font-bold">Reports / MIS</h1><p className="mt-1 text-sm text-slate-500">Operational summary from the live task data.</p></div><div className="grid gap-4 md:grid-cols-3"><div className="card p-5"><div className="text-xs text-slate-500">Total</div><div className="mt-2 text-3xl font-black">{d?.total_tasks??0}</div></div><div className="card p-5"><div className="text-xs text-slate-500">Completion rate</div><div className="mt-2 text-3xl font-black">{d?.completion_rate??0}%</div></div><div className="card p-5"><div className="text-xs text-slate-500">SLA breach rate</div><div className="mt-2 text-3xl font-black text-red-600">{d?.sla_breach_rate??0}%</div></div></div><div className="card p-5"><h2 className="font-bold">By status</h2><pre className="mt-4 overflow-auto rounded-lg bg-slate-950 p-4 text-xs text-slate-100">{JSON.stringify(d?.by_status??{},null,2)}</pre></div></div>}
