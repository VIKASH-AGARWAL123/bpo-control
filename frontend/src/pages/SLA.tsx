import {useEffect,useState} from 'react';import api from '../services/api';export default function SLA(){const [data,setData]=useState<any>();useEffect(()=>{api.get('/sla').then(r=>setData(r.data))},[]);return <div className="space-y-5"><div><h1 className="text-2xl font-bold">SLA Control Center</h1><p className="mt-1 text-sm text-slate-500">Monitor at-risk and breached work.</p></div><div className="grid gap-4 md:grid-cols-3"><div className="card p-5"><div className="text-sm text-slate-500">Met</div><div className="mt-2 text-3xl font-black">{data?.counts?.met??0}</div></div><div className="card p-5"><div className="text-sm text-slate-500">At Risk</div><div className="mt-2 text-3xl font-black text-orange-600">{data?.counts?.at_risk??0}</div></div><div className="card p-5"><div className="text-sm text-slate-500">Breached</div><div className="mt-2 text-3xl font-black text-red-600">{data?.counts?.breached??0}</div></div></div><div className="card overflow-hidden"><div className="border-b border-slate-200 p-5 font-bold">Attention queue</div><table className="w-full text-sm"><thead className="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th className="px-5 py-3">Task</th><th className="px-5 py-3">Priority</th><th className="px-5 py-3">SLA</th><th className="px-5 py-3">Due</th></tr></thead><tbody className="divide-y divide-slate-100">{data?.tasks?.map((t:any)=><tr key={t.id}><td className="px-5 py-4">{t.title}</td><td className="px-5 py-4">{t.priority}</td><td className="px-5 py-4">{t.sla_status}</td><td className="px-5 py-4">{t.due_at?new Date(t.due_at).toLocaleString():'—'}</td></tr>)}</tbody></table></div></div>}
